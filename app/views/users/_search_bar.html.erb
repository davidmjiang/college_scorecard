<%= form_tag schools_path, :method => 'get' do %>
  <%= label_tag :name, "Search by School" %>
  <%= text_field_tag :name %>
  <%= label_tag :region, "Region:"%>
  <%= select_tag(:region, options_for_select(school_regions), :mulitple => true) %>
  <%= label_tag :locale, "Locale:" %>
  <%= select_tag(:locale, options_for_select(locale_types), :mulitple => true) %>
  <%= submit_tag "Search" %>
<% end %>

<!-- Choosing a region will send its region_id to the params, which will go into the solr search. Choosing a locale will send 13 for city, 23 for suburb, 33 for town and 43 for rural. This will require some tranlating in the solr search since I've narrowed down the 12 categories into 4-->